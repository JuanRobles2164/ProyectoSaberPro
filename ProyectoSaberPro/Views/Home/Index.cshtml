@using ProyectoSaberPro.Models
@{
    ViewBag.Title = "Login";
}
@ViewBag.Message
@model LoginViewModel

    <center>
        <div class="card">
            <div class="row">
                <div class="col-md-12">
                    <section>
                        <from asp-controller="Home" asp-action="Index" method="post">
                            <h4>Iniciar Sesi&oacute;n</h4>
                            <br />
                            <div class="form-group">
                                <icon type="submit" class="btn btn-lg btn-outline-warning">@Html.ActionLink("Docente", "Login")</icon>
                            </div>
                            
                            <div class="from-group">
                                <icon type="submit" class="btn btn-lg btn-outline-success">@Html.ActionLink("Estudiante", "Login")</icon>
                            </div>
                            <br />
                            <div class="form-group">
                                <icon type="submit" class="btn btn-lg btn-outline-dark">@Html.ActionLink("Administrador", "")</icon>
                            </div>
                        </from>
                    </section>
                    <label id="IndexMessage" class="has-error"></label>
                </div>
            </div>
        </div>
    </center>
